# GitHub Push Notes

## 2026-04-23 — PAT 首次 push 握手

- 用临时 PAT + 完整 URL 直接 push 上去的 (3 commits: 9937ae4, 0588341, a5ab122)
- osxkeychain 里**没有**存 GitHub 凭据(URL 带 token 绕过了 credential helper)
- 本地 origin/main tracking ref 滞后于远端 3 个 commit,git status 会显示 "ahead 3",但远端其实已有
- 下次 git fetch / pull / push 会弹 credential 提示
- 届时填 username = JymWiltek + 新生成的 PAT,osxkeychain 会持久化
- 当时用过的 PAT 已在 Jym 端撤销
- 首次 fetch 之后 tracking ref 会自动对齐,ahead 3 会消失
